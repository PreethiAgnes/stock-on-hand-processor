apiVersion: v1
kind: Service
metadata: { name: {{ include "csv-processor.fullname" . }} }
spec:
  type: {{ .Values.service.type }}
  selector: { app.kubernetes.io/name: {{ include "csv-processor.name" . }} }
  ports:
    - { name: http, port: {{ .Values.service.port }}, targetPort: proxy }

